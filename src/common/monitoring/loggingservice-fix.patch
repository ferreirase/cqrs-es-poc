new LokiTransport({
    host: process.env.LOKI_URL || `http://${process.env.LOKI_HOST || 'localhost'}:${process.env.LOKI_PORT || 3100}`,
    labels: {
        app: 'cqrs-es-poc',
        service: 'transaction-service',
        environment: process.env.NODE_ENV || 'development',
    },
    // ... resto do código
}) 